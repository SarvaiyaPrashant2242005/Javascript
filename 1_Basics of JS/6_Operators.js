// Typesof Operators 

// datatypes
    // String, integer, number, booliens, undefined, null, bigInt, Symbol

// typeof shows the datatype of the variable

let age = 22;
let fn = "Prashant";

// console.log(typeof(age))
// console.log(typeof(fn))

// Coverting the number into the string

console.log(typeof (age + "") );
console.log(typeof(String(age)));

// Converting string into the number

console.log(typeof(+fn));
console.log(typeof(Number(fn)));

